# Google Colab에서 C++/CUDA 실행 - 작동 원리

## ❓ 질문: "노트북은 Python 아닌가요?"

**맞습니다!** Jupyter/Colab 노트북은 Python 환경입니다.

하지만 **C++/CUDA 프로젝트를 빌드하고 실행할 수 있습니다**.

---

## 🔧 작동 원리

### 1. Shell 명령어 실행

Python 셀에서 `!`를 붙이면 Linux shell 명령어를 실행할 수 있습니다:

```python
# Python 코드
import pandas as pd

# Shell 명령어 (! 붙임)
!nvidia-smi        # GPU 정보 확인
!ls -la            # 파일 목록
!g++ --version     # 컴파일러 버전
```

### 2. C++ 프로젝트 빌드

```python
# 노트북 셀에서:
!cmake ..          # CMake 설정 (shell 명령어)
!make -j4          # C++ 컴파일 (shell 명령어)
!ls -lh els_pricer # 생성된 실행 파일 확인
```

이렇게 하면 **C++ 실행 파일**이 생성됩니다.

### 3. C++ 실행 파일 실행

```python
# 노트북 셀에서:
!./els_pricer --compare   # C++/CUDA 실행 파일 실행
```

이 때 **실제 계산은 C++/CUDA 코드**에서 수행됩니다!
Python은 그냥 실행만 시켜주는 역할입니다.

### 4. 결과 분석

```python
# 이제 Python으로 결과 분석
import pandas as pd
import matplotlib.pyplot as plt

# C++이 생성한 CSV 읽기
df = pd.read_csv('benchmark_results.csv')

# 그래프 그리기
plt.plot(df['Time_sec'])
plt.show()
```

---

## 📊 전체 흐름

```
┌─────────────────────────────────────────────────────────┐
│  Google Colab (Python 환경)                              │
│                                                          │
│  Cell 1: !apt-get install cmake                         │
│          ↓ (shell 명령어 실행)                            │
│          CMake 설치 완료                                  │
│                                                          │
│  Cell 2: !cmake .. && make                              │
│          ↓ (C++ 컴파일)                                  │
│          els_pricer 실행 파일 생성                        │
│                                                          │
│  Cell 3: !./els_pricer --gpu-only                       │
│          ↓                                               │
│  ┌────────────────────────────────────────┐             │
│  │  els_pricer (C++/CUDA 실행 파일)        │             │
│  │  ← 실제 계산은 여기서 수행!              │             │
│  │  - GPU에서 ADI solver 실행              │             │
│  │  - 40M points 계산                      │             │
│  │  - 결과를 CSV 파일로 저장                │             │
│  └────────────────────────────────────────┘             │
│          ↓                                               │
│          benchmark_results.csv 생성                      │
│                                                          │
│  Cell 4: import pandas as pd                            │
│          df = pd.read_csv('benchmark_results.csv')      │
│          ↓ (Python으로 결과 분석)                         │
│          그래프 생성, 통계 계산                            │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

---

## 💡 핵심 포인트

### Python의 역할
- ✅ 환경 설정 (패키지 설치)
- ✅ 프로젝트 빌드 관리 (cmake, make 실행)
- ✅ C++ 실행 파일 실행
- ✅ 결과 분석 및 시각화

### C++/CUDA의 역할
- ✅ **실제 ELS 가격 계산** ← 중요!
- ✅ **GPU에서 병렬 계산**
- ✅ **ADI solver 실행**
- ✅ 성능 측정

### 성능은?

**100% C++/CUDA 성능입니다!**

Python은 단순히 실행만 시켜주고, 실제 계산은 컴파일된 C++ 바이너리가 GPU에서 직접 수행합니다.

```
Python overhead: ~0%
C++ 실행: 100% 네이티브 성능
GPU 가속: 100% CUDA 성능
```

---

## 🎯 왜 이 방식을 사용하나요?

### 장점

1. **편리함**: 웹 브라우저만으로 GPU 테스트 가능
2. **무료**: Colab GPU 무료 제공
3. **재현성**: 노트북 공유하면 누구나 같은 환경에서 실행 가능
4. **시각화**: Python matplotlib로 결과 바로 그래프화

### 대안과 비교

| 방법 | 설정 시간 | 비용 | Python 사용 |
|------|----------|------|-------------|
| **Colab 노트북** | 0분 | 무료 | 환경 관리만 |
| AWS EC2 직접 | 30분 | $0.5/h | 사용 안 함 |
| 로컬 GPU | 설정 필요 | 하드웨어 | 사용 안 함 |

---

## 📝 실제 예제

### 노트북 셀 예시

```python
# Cell 1: GPU 확인 (Python이 shell 실행)
!nvidia-smi

# Cell 2: C++ 빌드 (Python이 shell 실행)
!cd build && cmake .. && make -j4

# Cell 3: C++ 실행 (Python이 C++ 바이너리 실행)
!./build/els_pricer --compare

# Cell 4: 결과 분석 (Python 사용)
import pandas as pd
df = pd.read_csv('benchmark_results.csv')
print(f"Average time: {df['Time_sec'].mean():.3f}s")
```

### 실행 결과

```
# Cell 3 출력 (C++ 프로그램이 출력):
Grid: 200×200×1000
CPU: 0.683s
GPU: 0.050s
Speedup: 13.7×

# Cell 4 출력 (Python이 분석):
Average time: 0.732s
```

---

## ✅ 결론

**질문**: "노트북은 Python 아닌가요?"

**답변**: 맞습니다! 하지만:
- Python은 **오케스트레이터** 역할만
- 실제 계산은 **C++/CUDA 바이너리**가 수행
- 성능은 **100% 네이티브 C++/CUDA**

이것이 Colab에서 C++ 프로젝트를 테스트하는 **표준 방법**입니다.

---

## 🔗 참고

유사한 방식을 사용하는 프로젝트들:
- Google의 공식 CUDA 샘플
- PyTorch (C++ 백엔드를 Python으로 제어)
- TensorFlow (C++ 코어를 Python으로 사용)
- 머신러닝 벤치마크 노트북들

**우리 프로젝트도 같은 방식입니다!**
